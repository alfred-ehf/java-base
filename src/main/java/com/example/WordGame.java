package com.example;

import java.util.Map;

public class WordGame {
    private Map<Character, Integer> POINTS = Utils.getScoreMap();
    private Map<Character, Integer> DIST = Utils.getDistributionMap();
    private char[] ALPHABET = Utils.getEnglishAlphabet();

    public int scoreWord(String word) {
       return 0;
    }
}
